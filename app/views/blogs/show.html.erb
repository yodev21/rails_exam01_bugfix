<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @blog.content %>
</p>

<% unless @blog.comments.count.zero? %>
  <hr>

  <p>
    <strong>Comment:</strong>
    <!-- # エラー原因: 数値型に文字列型を繋げようとしていた -->
    <!-- # 修正の意図: Fix count.to_i => count.to_s -->
    <%= @blog.comments.count.to_s + "comments" %>
  </p>

  <p>
    <%= render @blog.comments %>
  </p>
<% end %>

<p>
  <%= render 'comments/form', blog: @blog %>
</p>

<!-- # エラー原因: シングルクォートまた、blogとなっていた -->
<!-- # 修正の意図: Fix  '/blogs/#{blog.id}/edit' => "/blogs/#{@blog.id}/edit" -->
<%= link_to 'Edit', "/blogs/#{@blog.id}/edit" %> |
<%= link_to 'Back', blogs_path %>
